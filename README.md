## Build

Prerequisites: Node.js 20+ (https://nodejs.org)

1. npm install
2. npm run dev        # Launch in dev mode (hot reload)
3. npm run build      # Compile to dist/ folder

Output: dist/xDMD-PPLX-Analyzer-Setup.exe and dist/xDMD-PPLX-Analyzer-Portable.exe
